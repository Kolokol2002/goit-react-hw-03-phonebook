import styled from 'styled-components';

const ContainerFilter = styled.div`
  margin-bottom: 30px;
`;

const TitleFilter = styled.h3``;

const InputFilter = styled.input``;

export { ContainerFilter, TitleFilter, InputFilter };
